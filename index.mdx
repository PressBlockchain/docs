---
title: "PressChain"
description: "The sovereign blockchain infrastructure for journalism. Public acceptance. Immutable evidence. On-chain truth."
mode: "custom"
---

<div className="pc-landing">

  {/* ── HERO ── */}
  <div className="pc-land-hero">
    <div className="pc-land-hero-inner">
      <div className="pc-land-badge">
        <span className="pc-land-badge-dot" />
        Sovereign · EVM-Compatible · Purpose-Built
      </div>

      <h1 className="pc-land-h1">
        The blockchain built<br/>for the <em>fourth estate</em>
      </h1>

      <p className="pc-land-lead">
        PressChain brings cryptographic accountability to journalism. Every published article passes public acceptance, carries committed evidence, and earns canonical status on a chain no one controls.
      </p>

      <div className="pc-land-ctas">
        <a href="/developer/introduction" className="pc-land-btn-solid">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>
          Developer Docs
        </a>
        <a href="/journalist/welcome" className="pc-land-btn-outline">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
          Journalist Guide
        </a>
      </div>
    </div>

    <div className="pc-land-hero-art">
      <div className="pc-land-orb pc-land-orb-1" />
      <div className="pc-land-orb pc-land-orb-2" />
      <div className="pc-land-capsule-card">
        <div className="pc-land-card-bar">
          <span className="pc-land-card-dot pc-land-card-dot-r" />
          <span className="pc-land-card-dot pc-land-card-dot-y" />
          <span className="pc-land-card-dot pc-land-card-dot-g" />
          <span className="pc-land-card-label">Capsule #1,847</span>
          <span className="pc-land-card-badge">Canonical</span>
        </div>
        <div className="pc-land-card-body">
          <div className="pc-land-card-title">City Council Approves $42M Budget</div>
          <div className="pc-land-card-meta">The Bay Tribune · Maya Chen</div>
          <div className="pc-land-card-row">
            <span className="pc-land-card-stat">
              <span className="pc-land-stat-num">91%</span>
              <span className="pc-land-stat-lab">Approval</span>
            </span>
            <span className="pc-land-card-stat">
              <span className="pc-land-stat-num">142</span>
              <span className="pc-land-stat-lab">Votes</span>
            </span>
            <span className="pc-land-card-stat">
              <span className="pc-land-stat-num">3</span>
              <span className="pc-land-stat-lab">Evidence</span>
            </span>
          </div>
          <div className="pc-land-card-tag">
            <span className="pc-land-tag-dot" />Integrity: Verified
          </div>
        </div>
      </div>
    </div>
  </div>

  {/* ── PILLARS ── */}
  <div className="pc-land-pillars">
    <div className="pc-land-pillar">
      <div className="pc-land-pillar-num">01</div>
      <div className="pc-land-pillar-title">No Deletion. Ever.</div>
      <p className="pc-land-pillar-desc">Canonical articles are immutable. Corrections create revision lineage. The public record of journalism is complete and permanent.</p>
    </div>
    <div className="pc-land-pillar">
      <div className="pc-land-pillar-num">02</div>
      <div className="pc-land-pillar-title">Public Acceptance</div>
      <p className="pc-land-pillar-desc">No outlet can approve its own work. Every article needs 60% weighted approval and 5% quorum in a 72-hour on-chain vote.</p>
    </div>
    <div className="pc-land-pillar">
      <div className="pc-land-pillar-num">03</div>
      <div className="pc-land-pillar-title">Evidence on Chain</div>
      <p className="pc-land-pillar-desc">Source documents are hash-committed at publication. Tampering is provable. Sources cannot be denied or retroactively erased.</p>
    </div>
    <div className="pc-land-pillar">
      <div className="pc-land-pillar-num">04</div>
      <div className="pc-land-pillar-title">Process, Not Censorship</div>
      <p className="pc-land-pillar-desc">Challenges go to the Court engine with bonded jurors and claim-level evidence. Verdicts assign permanent integrity labels.</p>
    </div>
  </div>

  {/* ── PATHS ── */}
  <div className="pc-land-section-title">Where would you like to start?</div>

  <div className="pc-land-paths">
    <a href="/developer/introduction" className="pc-land-path pc-land-path-dev">
      <div className="pc-land-path-eyebrow">Engineers</div>
      <div className="pc-land-path-name">Build on PressChain</div>
      <p className="pc-land-path-desc">EVM-compatible L1, full contract registry, Bridge API, PressKey integration, and operator deployment guides.</p>
      <div className="pc-land-path-links">
        <a href="/developer/quickstart">Quickstart</a>
        <a href="/developer/chain/contracts">Contracts</a>
        <a href="/developer/integrations/bridge-api">Bridge API</a>
        <a href="/developer/capsule/standard">Capsule Schema</a>
      </div>
      <div className="pc-land-path-arrow">→</div>
    </a>

    <a href="/journalist/welcome" className="pc-land-path pc-land-path-jrn">
      <div className="pc-land-path-eyebrow">Journalists & Reporters</div>
      <div className="pc-land-path-name">Publish and Protect</div>
      <p className="pc-land-path-desc">Understand Capsules, acceptance voting, evidence commitment, rights licensing, and how to earn from your journalism.</p>
      <div className="pc-land-path-links">
        <a href="/journalist/capsules/what-is-a-capsule">What Is a Capsule?</a>
        <a href="/journalist/voting/how-voting-works">How Voting Works</a>
        <a href="/journalist/court/disputes">Disputes & Court</a>
        <a href="/journalist/money/rights-licensing">Rights Licensing</a>
      </div>
      <div className="pc-land-path-arrow">→</div>
    </a>

    <a href="/journalist/outlets/register-your-newsroom" className="pc-land-path pc-land-path-out">
      <div className="pc-land-path-eyebrow">Publishers & Newsrooms</div>
      <div className="pc-land-path-name">Register Your Outlet</div>
      <p className="pc-land-path-desc">Onboard your newsroom, verify your domain, connect your CMS, and access Press Network distribution and the rights marketplace.</p>
      <div className="pc-land-path-links">
        <a href="/journalist/outlets/register-your-newsroom">Onboarding</a>
        <a href="/journalist/outlets/domain-verification">Domain Verification</a>
        <a href="/journalist/outlets/wordpress-guide">WordPress Plugin</a>
        <a href="/journalist/outlets/syndication">Distribution</a>
      </div>
      <div className="pc-land-path-arrow">→</div>
    </a>
  </div>

  {/* ── FEATURES ── */}
  <div className="pc-land-features">
    <div className="pc-land-feat">
      <div className="pc-land-feat-icon">⚡</div>
      <div className="pc-land-feat-name">EVM-Compatible L1</div>
      <p className="pc-land-feat-desc">Full Ethereum toolchain. Solidity contracts, ethers.js, viem, any EVM wallet. Deploy today.</p>
    </div>
    <div className="pc-land-feat">
      <div className="pc-land-feat-icon">📦</div>
      <div className="pc-land-feat-name">The Capsule Standard</div>
      <p className="pc-land-feat-desc">Journalism as a protocol object. Claims, evidence, contributors, lifecycle, and economic routing in one primitive.</p>
    </div>
    <div className="pc-land-feat">
      <div className="pc-land-feat-icon">🔑</div>
      <div className="pc-land-feat-name">PressKey Identity</div>
      <p className="pc-land-feat-desc">Every action is locally signed. Keys never leave the device. Replay attacks are structurally impossible.</p>
    </div>
    <div className="pc-land-feat">
      <div className="pc-land-feat-icon">🌐</div>
      <div className="pc-land-feat-name">Bridge API</div>
      <p className="pc-land-feat-desc">REST middleware normalizes chain data. Capsule submission, voting, court, and distribution behind clean endpoints.</p>
    </div>
    <div className="pc-land-feat">
      <div className="pc-land-feat-icon">⚖️</div>
      <div className="pc-land-feat-name">Court Engine</div>
      <p className="pc-land-feat-desc">Claim-level disputes with bonded jurors, evidence windows, and permanent integrity labels. Process replaces censorship.</p>
    </div>
    <div className="pc-land-feat">
      <div className="pc-land-feat-icon">💸</div>
      <div className="pc-land-feat-name">Rights Marketplace</div>
      <p className="pc-land-feat-desc">On-chain licensing with basis-point revenue splits. Instant settlement. No intermediary. No platform cut.</p>
    </div>
  </div>

  {/* ── BOTTOM CTA ── */}
  <div className="pc-land-cta">
    <div className="pc-land-cta-content">
      <div className="pc-land-cta-title">Ready to get started?</div>
      <p className="pc-land-cta-desc">Connect to the PressChain testnet in minutes. Free testnet PRESS from the faucet. Full documentation for every integration path.</p>
      <div className="pc-land-ctas">
        <a href="/developer/quickstart" className="pc-land-btn-solid">Start Building</a>
        <a href="https://faucet.presschain.io" className="pc-land-btn-outline" target="_blank" rel="noopener">Get Testnet PRESS</a>
        <a href="https://portal.presschain.io" className="pc-land-btn-ghost" target="_blank" rel="noopener">Open Portal →</a>
      </div>
    </div>
  </div>

</div>

<style>{`
/* ═══════════════════════════════════════════════════
   LANDING PAGE — Self-contained styles
   All values defined inline so dark mode responds
   to both CSS vars AND Mintlify's html class
═══════════════════════════════════════════════════ */

/* Tokens (light) */
.pc-landing {
  --l-bg:        #F8FCFF;
  --l-card:      #FFFFFF;
  --l-border:    rgba(0,43,67,0.08);
  --l-border-b:  rgba(0,155,249,0.20);
  --l-ink:       #002A43;
  --l-mid:       #004F84;
  --l-muted:     #4A6B82;
  --l-sky:       #009BF9;
  --l-pale:      #EEF7FF;
  --l-hero-from: #EAF5FF;
  --l-hero-to:   #F6FBFF;
  --l-glow:      rgba(0,155,249,0.08);
  --l-shadow:    0 2px 16px rgba(0,155,249,0.10), 0 1px 4px rgba(0,43,67,0.06);
  --l-shadow-lg: 0 8px 40px rgba(0,155,249,0.14), 0 2px 8px rgba(0,43,67,0.08);
  font-family: 'Nunito', system-ui, sans-serif;
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 4px;
}

/* Dark token override — covers Mintlify's selectors */
.dark .pc-landing,
html.dark .pc-landing,
[data-theme="dark"] .pc-landing,
[data-mode="dark"] .pc-landing {
  --l-bg:        #040E18;
  --l-card:      #060F1A;
  --l-border:    rgba(0,155,249,0.10);
  --l-border-b:  rgba(0,155,249,0.18);
  --l-ink:       #D8EEFF;
  --l-mid:       #93C5E8;
  --l-muted:     #6A9AB8;
  --l-sky:       #38B6FF;
  --l-pale:      #061827;
  --l-hero-from: #030C16;
  --l-hero-to:   #06111E;
  --l-glow:      rgba(0,155,249,0.12);
  --l-shadow:    0 2px 16px rgba(0,0,0,0.4), 0 1px 4px rgba(0,155,249,0.06);
  --l-shadow-lg: 0 8px 40px rgba(0,0,0,0.5), 0 2px 8px rgba(0,155,249,0.10);
}

/* ── HERO ── */
.pc-land-hero {
  position: relative;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 48px;
  align-items: center;
  border-radius: 24px;
  border: 1px solid var(--l-border-b);
  background: linear-gradient(145deg, var(--l-hero-from) 0%, var(--l-hero-to) 100%);
  padding: 64px 56px 60px;
  margin-bottom: 40px;
  overflow: hidden;
}

.pc-land-hero::before {
  content: '';
  position: absolute;
  top: -120px; right: -120px;
  width: 560px; height: 560px;
  border-radius: 50%;
  background: radial-gradient(circle, var(--l-glow) 0%, transparent 65%);
  pointer-events: none;
}

@media (max-width: 840px) {
  .pc-land-hero { grid-template-columns: 1fr; padding: 44px 32px; }
  .pc-land-hero-art { display: none; }
}

/* Badge */
.pc-land-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-size: 0.7rem;
  font-weight: 800;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--l-sky);
  background: rgba(0,155,249,0.08);
  border: 1px solid rgba(0,155,249,0.22);
  border-radius: 999px;
  padding: 4px 14px 4px 10px;
  margin-bottom: 22px;
}
.pc-land-badge-dot {
  width: 6px; height: 6px;
  border-radius: 50%;
  background: var(--l-sky);
  box-shadow: 0 0 6px rgba(0,155,249,0.8);
  animation: land-blink 2.4s ease-in-out infinite;
}
@keyframes land-blink {
  0%,100% { opacity:1; } 50% { opacity:0.4; }
}

/* Headline */
.pc-land-h1 {
  font-family: 'DM Serif Display', Georgia, serif !important;
  font-size: clamp(2.2rem, 4.5vw, 3.6rem);
  line-height: 1.06;
  color: var(--l-ink);
  margin: 0 0 18px;
  letter-spacing: -0.02em;
}
.pc-land-h1 em { font-style: italic; color: var(--l-sky); }

.pc-land-lead {
  font-size: 1.06rem;
  font-weight: 500;
  line-height: 1.72;
  color: var(--l-mid);
  max-width: 520px;
  margin: 0 0 32px;
}

/* CTA row */
.pc-land-ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  align-items: center;
}

.pc-land-btn-solid {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: var(--l-ink);
  color: #fff !important;
  font-weight: 700;
  font-size: 0.93rem;
  padding: 13px 26px;
  border-radius: 10px;
  text-decoration: none !important;
  box-shadow: 0 4px 16px rgba(0,43,67,0.28);
  transition: transform 0.18s ease, box-shadow 0.18s ease, background 0.18s ease;
}
.dark .pc-land-btn-solid,
html.dark .pc-land-btn-solid,
[data-theme="dark"] .pc-land-btn-solid {
  background: var(--l-sky);
  box-shadow: 0 4px 20px rgba(0,155,249,0.35);
}
.pc-land-btn-solid:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 28px rgba(0,43,67,0.35);
}

.pc-land-btn-outline {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: transparent;
  color: var(--l-sky) !important;
  font-weight: 700;
  font-size: 0.93rem;
  padding: 12px 24px;
  border-radius: 10px;
  border: 1.5px solid rgba(0,155,249,0.28);
  text-decoration: none !important;
  transition: transform 0.18s ease, border-color 0.18s ease, background 0.18s ease;
}
.pc-land-btn-outline:hover {
  transform: translateY(-2px);
  border-color: var(--l-sky);
  background: rgba(0,155,249,0.06);
}

.pc-land-btn-ghost {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: transparent;
  color: var(--l-muted) !important;
  font-weight: 600;
  font-size: 0.88rem;
  padding: 12px 16px;
  border-radius: 10px;
  text-decoration: none !important;
  transition: color 0.18s ease;
}
.pc-land-btn-ghost:hover { color: var(--l-sky) !important; }

/* ── HERO ART (Capsule card mockup) ── */
.pc-land-hero-art {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}
.pc-land-orb {
  position: absolute;
  border-radius: 50%;
  pointer-events: none;
}
.pc-land-orb-1 {
  width: 220px; height: 220px;
  background: radial-gradient(circle, rgba(0,155,249,0.12) 0%, transparent 70%);
  top: -20px; right: -20px;
}
.pc-land-orb-2 {
  width: 160px; height: 160px;
  background: radial-gradient(circle, rgba(0,43,67,0.08) 0%, transparent 70%);
  bottom: -10px; left: 0;
}
.pc-land-capsule-card {
  position: relative;
  width: 100%;
  max-width: 340px;
  background: var(--l-card);
  border: 1px solid var(--l-border-b);
  border-radius: 16px;
  box-shadow: var(--l-shadow-lg);
  overflow: hidden;
}
.pc-land-card-bar {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 12px 16px;
  background: rgba(0,155,249,0.04);
  border-bottom: 1px solid var(--l-border);
}
.pc-land-card-dot {
  width: 8px; height: 8px; border-radius: 50%;
}
.pc-land-card-dot-r { background: #FF5F56; }
.pc-land-card-dot-y { background: #FFBD2E; }
.pc-land-card-dot-g { background: #27C93F; }
.pc-land-card-label {
  font-family: 'DM Mono', monospace;
  font-size: 0.7rem;
  color: var(--l-muted);
  margin-left: 4px;
  flex: 1;
}
.pc-land-card-badge {
  font-size: 0.65rem;
  font-weight: 800;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: #14B874;
  background: rgba(20,184,116,0.10);
  border: 1px solid rgba(20,184,116,0.24);
  border-radius: 999px;
  padding: 2px 8px;
}
.pc-land-card-body { padding: 18px 18px 16px; }
.pc-land-card-title {
  font-family: 'DM Serif Display', serif;
  font-size: 0.98rem;
  color: var(--l-ink);
  margin-bottom: 4px;
  line-height: 1.3;
}
.pc-land-card-meta {
  font-size: 0.72rem;
  color: var(--l-muted);
  margin-bottom: 14px;
}
.pc-land-card-row {
  display: flex;
  gap: 16px;
  margin-bottom: 14px;
}
.pc-land-card-stat {
  display: flex;
  flex-direction: column;
  gap: 1px;
}
.pc-land-stat-num {
  font-family: 'DM Mono', monospace;
  font-size: 1.1rem;
  font-weight: 500;
  color: var(--l-sky);
}
.pc-land-stat-lab {
  font-size: 0.65rem;
  color: var(--l-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.pc-land-card-tag {
  display: flex;
  align-items: center;
  gap: 7px;
  font-size: 0.73rem;
  font-weight: 700;
  color: var(--l-sky);
  padding: 6px 10px;
  background: rgba(0,155,249,0.06);
  border-radius: 7px;
  border: 1px solid rgba(0,155,249,0.14);
}
.pc-land-tag-dot {
  width: 6px; height: 6px;
  border-radius: 50%;
  background: var(--l-sky);
}

/* ── PILLARS ── */
.pc-land-pillars {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1px;
  background: var(--l-border);
  border-radius: 16px;
  overflow: hidden;
  border: 1px solid var(--l-border);
  margin-bottom: 56px;
}
@media (max-width: 768px) {
  .pc-land-pillars { grid-template-columns: 1fr 1fr; }
}
.pc-land-pillar {
  background: var(--l-card);
  padding: 28px 24px;
}
.pc-land-pillar-num {
  font-family: 'DM Mono', monospace;
  font-size: 0.68rem;
  color: var(--l-sky);
  letter-spacing: 0.08em;
  margin-bottom: 12px;
  display: block;
}
.pc-land-pillar-title {
  font-family: 'DM Serif Display', serif;
  font-size: 1.05rem;
  color: var(--l-ink);
  margin-bottom: 8px;
}
.pc-land-pillar-desc {
  font-size: 0.84rem;
  line-height: 1.65;
  color: var(--l-muted);
  margin: 0;
}

/* ── SECTION LABEL ── */
.pc-land-section-title {
  font-family: 'DM Serif Display', serif;
  font-size: 1.5rem;
  color: var(--l-ink);
  margin-bottom: 20px;
  letter-spacing: -0.02em;
}

/* ── PATHS (3 col) ── */
.pc-land-paths {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 14px;
  margin-bottom: 56px;
}
@media (max-width: 768px) {
  .pc-land-paths { grid-template-columns: 1fr; }
}

.pc-land-path {
  position: relative;
  border-radius: 18px;
  padding: 30px 26px 26px;
  border: 1px solid var(--l-border);
  background: var(--l-card);
  text-decoration: none !important;
  display: block;
  transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
  overflow: hidden;
}
.pc-land-path:hover {
  transform: translateY(-3px);
  box-shadow: var(--l-shadow-lg);
  border-color: rgba(0,155,249,0.28);
}
.pc-land-path-dev {
  background: linear-gradient(145deg, rgba(0,43,67,0.96) 0%, rgba(0,61,96,0.96) 100%);
  border-color: rgba(0,80,130,0.5);
}
.dark .pc-land-path-dev,
html.dark .pc-land-path-dev,
[data-theme="dark"] .pc-land-path-dev {
  background: linear-gradient(145deg, #040D18 0%, #071525 100%);
  border-color: rgba(0,155,249,0.14);
}

.pc-land-path-eyebrow {
  font-size: 0.69rem;
  font-weight: 800;
  letter-spacing: 0.09em;
  text-transform: uppercase;
  color: var(--l-sky);
  margin-bottom: 7px;
}
.pc-land-path-name {
  font-family: 'DM Serif Display', serif;
  font-size: 1.3rem;
  color: var(--l-ink);
  margin-bottom: 10px;
  line-height: 1.2;
}
.pc-land-path-dev .pc-land-path-name { color: #D8EEFF; }

.pc-land-path-desc {
  font-size: 0.86rem;
  line-height: 1.65;
  color: var(--l-muted);
  margin-bottom: 18px;
}
.pc-land-path-dev .pc-land-path-desc { color: rgba(216,238,255,0.60); }

.pc-land-path-links {
  display: flex;
  flex-direction: column;
  gap: 5px;
  margin-bottom: 20px;
}
.pc-land-path-links a {
  font-size: 0.82rem;
  font-weight: 600;
  color: var(--l-sky) !important;
  text-decoration: none !important;
  border-bottom: none !important;
  transition: opacity 0.15s ease;
  padding: 2px 0;
}
.pc-land-path-links a:hover { opacity: 0.7; }
.pc-land-path-dev .pc-land-path-links a { color: #63C8FF !important; }

.pc-land-path-arrow {
  position: absolute;
  bottom: 24px; right: 24px;
  font-size: 1.3rem;
  color: var(--l-sky);
  opacity: 0.4;
  transition: opacity 0.2s ease, transform 0.2s ease;
}
.pc-land-path:hover .pc-land-path-arrow {
  opacity: 1;
  transform: translateX(3px);
}

/* ── FEATURES ── */
.pc-land-features {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
  margin-bottom: 56px;
}
@media (max-width: 768px) {
  .pc-land-features { grid-template-columns: 1fr 1fr; }
}
@media (max-width: 480px) {
  .pc-land-features { grid-template-columns: 1fr; }
}

.pc-land-feat {
  border-radius: 14px;
  border: 1px solid var(--l-border);
  background: var(--l-card);
  padding: 22px 20px;
  transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease;
}
.pc-land-feat:hover {
  transform: translateY(-2px);
  box-shadow: var(--l-shadow);
  border-color: rgba(0,155,249,0.22);
}
.pc-land-feat-icon {
  font-size: 1.4rem;
  display: block;
  margin-bottom: 10px;
}
.pc-land-feat-name {
  font-family: 'DM Serif Display', serif;
  font-size: 0.98rem;
  color: var(--l-ink);
  margin-bottom: 6px;
}
.pc-land-feat-desc {
  font-size: 0.83rem;
  line-height: 1.62;
  color: var(--l-muted);
  margin: 0;
}

/* ── BOTTOM CTA ── */
.pc-land-cta {
  border-radius: 20px;
  border: 1px solid rgba(0,155,249,0.20);
  background: linear-gradient(145deg, var(--l-hero-from) 0%, var(--l-hero-to) 100%);
  padding: 52px 52px 48px;
  text-align: center;
  position: relative;
  overflow: hidden;
  margin-bottom: 8px;
}
.pc-land-cta::before {
  content: '';
  position: absolute;
  top: -80px; left: 50%;
  transform: translateX(-50%);
  width: 400px; height: 300px;
  border-radius: 50%;
  background: radial-gradient(circle, var(--l-glow) 0%, transparent 70%);
  pointer-events: none;
}
.pc-land-cta-title {
  font-family: 'DM Serif Display', serif;
  font-size: clamp(1.6rem, 3vw, 2.2rem);
  color: var(--l-ink);
  margin-bottom: 10px;
  position: relative;
}
.pc-land-cta-desc {
  font-size: 0.97rem;
  color: var(--l-mid);
  line-height: 1.68;
  max-width: 520px;
  margin: 0 auto 30px;
  position: relative;
}
.pc-land-cta .pc-land-ctas {
  justify-content: center;
  position: relative;
}
`}</style>
